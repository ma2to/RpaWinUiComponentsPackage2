<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RpaWinUiComponentsPackage.AdvancedWinUiDataGrid.Modules.Table.Controls.AdvancedDataGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <!-- Templates will be set programmatically from code - NO hardcoded colors! -->
        <!-- Default cell template - colors set from DataGridColorConfig -->
        <DataTemplate x:Key="DefaultCellTemplate">
            <Border x:Name="CellBorder"
                    BorderThickness="1"
                    Padding="8,4">
                <TextBlock x:Name="CellText" Text="Cell Placeholder" />
            </Border>
        </DataTemplate>

        <!-- Header template - colors set from DataGridColorConfig -->
        <DataTemplate x:Key="HeaderTemplate">
            <Border x:Name="HeaderBorder"
                    BorderThickness="0,0,1,1"
                    Padding="8,4">
                <TextBlock x:Name="HeaderText" 
                           Text="Header Placeholder"
                           FontWeight="SemiBold" />
            </Border>
        </DataTemplate>

    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header row -->
            <RowDefinition Height="*" />    <!-- Data area -->
        </Grid.RowDefinitions>

        <!-- Column Headers -->
        <ScrollViewer x:Name="HeaderScrollViewer"
                      Grid.Row="0"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Disabled"
                      ZoomMode="Disabled">
            <ItemsRepeater x:Name="HeaderRepeater"
                           ItemTemplate="{StaticResource HeaderTemplate}" />
        </ScrollViewer>

        <!-- Data Grid Area -->
        <ScrollViewer x:Name="DataScrollViewer"
                      Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      ZoomMode="Disabled">
            <ItemsRepeater x:Name="DataRepeater"
                           ItemTemplate="{StaticResource DefaultCellTemplate}" />
        </ScrollViewer>

        <!-- Loading indicator -->
        <ProgressRing x:Name="LoadingRing"
                      Grid.Row="1"
                      IsActive="False"
                      Visibility="Collapsed"
                      Width="50"
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center" />

        <!-- Status bar - colors set programmatically from DataGridColorConfig -->
        <Border x:Name="StatusBar"
                Grid.Row="1"
                VerticalAlignment="Bottom"
                BorderThickness="0,1,0,0"
                Padding="8,4"
                Visibility="Collapsed">
            <TextBlock x:Name="StatusText"
                       Text="Ready"
                       FontSize="12" />
        </Border>
    </Grid>
</UserControl>